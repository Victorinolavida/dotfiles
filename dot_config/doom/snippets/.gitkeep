# Doom's `snippets' module expects this directory to exist for personal
# YASnippet snippets (~/.config/doom/snippets/). Drop <major-mode>/<name>
# snippet files in here; this placeholder just keeps the empty dir tracked.
